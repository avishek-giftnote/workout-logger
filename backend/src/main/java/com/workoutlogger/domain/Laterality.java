package com.workoutlogger.domain;

/** BILATERAL = limbs fixed together (barbell); ISOLATERAL = independent but simultaneous (dumbbells);
 *  UNILATERAL = one side at a time. */
public enum Laterality { BILATERAL, ISOLATERAL, UNILATERAL }
