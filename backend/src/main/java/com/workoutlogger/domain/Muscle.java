package com.workoutlogger.domain;

/** Muscle groups for per-muscle volume tracking (see docs/coach.md). */
public enum Muscle {
    CHEST, FRONT_DELT, SIDE_DELT, REAR_DELT, LAT, UPPER_BACK, TRAP,
    BICEP, TRICEP, FOREARM, QUAD, HAMSTRING, GLUTE, CALF, ABS
}
